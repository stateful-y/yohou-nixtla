---
description: Release history for every published version, newest first.
---

# Changelog

<!--
The H1 above is this page's own, and the include starts at line 3 to skip the
one git-cliff writes into CHANGELOG.md (see the `header` in .git-cliff.toml).
Without an H1 in *this* file, the <!-- SUBPAGES --> hook in docs/hooks.py reads
the raw source, finds no heading, and drops the changelog from the Reference
index with a warning that fails `mkdocs build --strict`.
-->

--8<-- "CHANGELOG.md:3"
